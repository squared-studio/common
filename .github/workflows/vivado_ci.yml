name: Vivado CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  CI:

    runs-on: [make, vivado]

    steps:
    - uses: actions/checkout@v4

    - name: Simulate
      shell: bash
      run:
        make CI

    - name: Error Log
      shell: bash
      run:
        grep -r "FAIL" ./___CI_REPORT | tee errors.txt

    - name: Upload a Build Artifact
      uses: actions/upload-artifact@v4.3.1
      with:
        path: ___CI_REPORT

    - name: Fail if Simulation Contains Fails
      shell: bash
      run:
        if [[ -s errors.txt ]]; then
          echo "Simulation Contains Fails"
          exit 1
        fi

      
